import SwiftUI

struct AppColors {

    // Primary UNCW-inspired colors
    static let teal = Color(red: 0/255, green: 111/255, blue: 113/255)

    static let navy = Color(red: 0/255, green: 63/255, blue: 92/255)

    // Backgrounds
    static let background = Color(.systemGroupedBackground)

    static let card = Color(.secondarySystemGroupedBackground)

    // Text
    static let primaryText = Color.primary

    static let secondaryText = Color.secondary

    // Risk colors
    static let lowRisk = teal

    static let moderateRisk = Color.orange

    static let elevatedRisk = Color.red
}
